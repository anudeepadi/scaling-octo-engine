project_prompt = """
When working on this project, ensure that Gemini quick replies UI enhancements are distinct, intuitive, and dynamically generated based on the Gemini response context.
"""

[commands]
format = ["flutter", "format", "."]
test = ["flutter", "test"]

[tool_docs]
format = "Formats the entire Flutter project following Dart and Flutter style guidelines."
test = "Runs Flutter unit and integration tests. Accepts optional arguments for specific test targets."